spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/teacher_on_demand
    username: teacher
    password: teacher123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

security:
  salt: teacherOnDemand2026